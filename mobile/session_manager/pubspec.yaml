name: session_manager
description: "Session Manager — local credentials and QR transfer MVP."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  flutter_secure_storage: ^9.2.4
  encrypt: ^5.0.3
  uuid: ^4.5.1
  shelf: ^1.4.2
  http: ^1.2.2
  mobile_scanner: ^6.0.2
  network_info_plus: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
