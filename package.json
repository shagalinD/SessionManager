{
  "name": "password-manager",
  "version": "1.0.0",
  "description": "Password Manager with QR code",
  "scripts": {
    "client": "node start-client.js",
    "server": "cd server && node server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}